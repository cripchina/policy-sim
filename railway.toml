[build]
  builder = "DOCKERFILE"
  dockerfilePath = "Dockerfile"

[deploy]
  restartPolicyType = "ALWAYS"
  healthcheckPath = "/api/health"
  healthcheckTimeout = 100

[[servicePorts]]
  port = 3000
  protocol = "TCP"
